# Ask the user to type in a string
user_input = input("Please type in a string: ")

# Iterate through each character in the string
for char in user_input:
    # Print the character followed by a star on a new line
    print(char)
    print('*')
